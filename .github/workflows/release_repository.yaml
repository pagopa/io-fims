name: (Release) Repository Settings

on:
  workflow_dispatch:
  push:
    paths:
      - "infra/repository/**"

jobs:
  release:
    uses: ./.github/workflows/call_release_infra.yaml
    name: Terraform Plan
    secrets: inherit
    with:
      environment: 'prod'
      dir: 'infra/repository'
