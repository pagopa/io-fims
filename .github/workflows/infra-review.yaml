name: PR Infrastructure Plan

on:
  workflow_dispatch:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
    paths:
      - ".github/workflows/infra-**.yaml"
      - "infra/resources/**"

jobs:
  infra_review:
    uses: pagopa/dx/.github/workflows/infra_plan.yaml@6f94721b2d7f9259aed464a5d034a60f7c605640
    name: PR Prod WestEurope
    secrets: inherit
    with:
      environment: prod
      base_path: infra/resources
      use_private_agent: true
