{
  "name": "io-fims",
  "type": "module",
  "packageManager": "yarn@4.1.1",
  "license": "UNLICENSED",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "code-review": "turbo run typecheck format:check lint:check test",
    "format": "turbo run format",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "prettier": "3.2.5",
    "turbo": "^1.13.2",
    "typescript": "^5.4.2"
  }
}
