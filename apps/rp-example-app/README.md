# rp-example-app

## Setup

1. Install the dependencies:

   ```sh
   yarn install --frozen-lockfile
   ```

2. Copy the example environment file:

   ```sh
   cp .env.example .env
   ```

3. Build the project:

   ```sh
   yarn build
   ```

4. Start the project:

   ```sh
   yarn start:dev
   ```
