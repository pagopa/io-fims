<%- include('partials/header_bootstrap') -%>

<div class="container">

<h1 class="text-4xl"><%= process.env.RELYING_PARTY_NAME || "Comune di Test" %></h1>

<% if (locals.isAuthenticated) { %>
    <p>You are logged in. <a href="/profile" class="underline">View your profile</a>.</p>
<% } else { %>
    <p>You are not logged in. <a href="/login" class="underline">Login</a>.</p>
<% } %>

</div>

<%- include('partials/footer_bootstrap') -%>
